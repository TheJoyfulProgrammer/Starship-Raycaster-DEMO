Place TrueType/OpenType fonts here.

Default expected font name (when we wire FreeType text rendering):
  Oxanium-VariableFont_wght.ttf

You can swap fonts later; this directory is intentionally empty in the repo/zip.
